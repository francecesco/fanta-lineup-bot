# Segreti / credenziali — MAI committare
.env

# Config con i tuoi identificativi di lega
config.py

# Spec formazione reale (usa formazione.example.json come modello)
formazione.json

# File dati personali (rosa, listone quotazioni, piani/strategie)
*.xlsx
*.xls

# Cattura di rete: contiene password e token in chiaro
*.har

# Ambiente Python
.venv/
__pycache__/
*.pyc

# Sistema
.DS_Store
*.bak

# --- File di lavoro personali: solo locale, MAI pubblicati ---
# Skill personalizzata con le preferenze reali (il template pubblico è in skill/)
.claude/
# Spec di design interni (contengono nome lega e nomi giocatori reali)
docs/superpowers/
# Roadmap/storico dettagliato con dati reali
ROADMAP_autoformazione.md
# Test che colpiscono gli endpoint reali (i test pubblici stanno in tests/)
/test_*.py
