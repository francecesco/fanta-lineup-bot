# tzdata: database fusi orari per zoneinfo (le immagini slim non lo includono sempre).
# È l'unica dipendenza del BOT autonomo: fanta_api, build_payload, RosterSito e il
# client Telegram sono stdlib puro, e la rosa arriva dai dati del sito (niente Excel).
tzdata
# openpyxl: opzionale, serve SOLO al flusso manuale basato su Excel (RosterMap +
# skill consiglia-formazione che legge rosa.xlsx). Il bot non lo usa.
openpyxl>=3.1
